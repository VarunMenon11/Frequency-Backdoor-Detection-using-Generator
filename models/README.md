# Models

This module will contain image classifiers used for:

- suspicious model training
- backdoor verification
- repaired model fine-tuning

The initial target will be a CIFAR-appropriate classifier such as a ResNet
variant.
