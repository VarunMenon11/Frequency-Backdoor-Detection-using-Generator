# Configs

Configuration files define the experimental state: dataset, poisoning ratio,
target label, trigger parameters, model choice, optimizer settings, random seed,
and output paths.

Keeping these values in files makes experiments reproducible and comparable.
