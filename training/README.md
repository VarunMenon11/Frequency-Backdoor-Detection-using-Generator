# Training

This module will contain training loops for:

- suspicious classifier training
- generator training
- repaired model fine-tuning

Training code is separated from model definitions so that experiments remain
auditable and configurable.
