# Poisoning

This module will implement controlled backdoor construction.

For Phase 3, it creates a simple configurable frequency-domain trigger, applies
it to a selected percentage of non-target-label images, and generates the
corresponding poisoned labels.

The goal is to build a measurable failure condition, not to assume any defense
will work.
